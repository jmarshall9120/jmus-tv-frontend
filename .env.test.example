# Copy to .env.test and fill in local test credentials.
# Used by: npm run test:auth and npm run test:api (SRP auth for Stryker API).
COGNITO_TEST_USER='your-test-user@example.com'
COGNITO_TEST_PASSWORD='your-test-password'
